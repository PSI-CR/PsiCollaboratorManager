class Schedule {
    constructor(scheduleId, name, workingDayId, scheduleDailys) {
        this.ScheduleId = scheduleId;
        this.Name = name;
        this.WorkingDayId = workingDayId;
        this.ScheduleDailys = scheduleDailys;
    }
}